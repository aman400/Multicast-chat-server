It is a multicast chat server implemented using JAVA.
This can be used to chat with mutiple clients over a LAN.


You just need to run server program over one computer in a LAN.
Other computers just need to run client programs which can automatically scan over the network(Manual address can be provided by the client)
for server and clients can login and chat with others.
